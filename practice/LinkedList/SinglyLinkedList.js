class Node {
  #value;
  #next;

  constructor(value, next) {
    this.#value = value;
    this.#next = next;
  }
}

class SinglyLinkedList {
  // do something...!
}

export default SinglyLinkedList;
